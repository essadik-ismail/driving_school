<div class="profile-image {{ $col ?? "" }}">

    <div class="img-uploader-content">
        <input 
            type="file" 
            class="filepond" 
            name="{{ $name }}"
            accept="image/png, image/jpeg, image/gif"
        />
    </div>

</div>